name: Test

on: [push]

jobs:
  compiler:
    name: Test Compiler
    runs-on: ubuntu-latest
    container: ghcr.io/cucapra/filament:latest
    steps:
      - name: Checkout commit that triggered runa
        uses: actions/checkout@v3
        with:
          repository: cucapra/filament
          ref: ${{ github.sha }}
      - name: Build Filament
        uses: actions-rs/cargo@v1
        with:
          command: build

      - name: Original test suite
        run:
          runt -d -o fail -j 1 --max-futures 10