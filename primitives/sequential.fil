import "./core.fil";

comp Mult<G: 2>(
  @interface[G] go_G: 1,
  @[G, G+1] left: 32,
  @[G, G+1] right: 32,
) -> (
  @[G+2, G+3] out: 32,
) {
  ll := new Register[32]<G, G+3>(left);
  lr := new Register[32]<G, G+3>(right);
  m := new MultComb[32]<G+2>(ll.out, lr.out);
  out = m.out;
}