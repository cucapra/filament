// Test basic conditional constraint syntax
comp Cond[A, B]<'G: 1>() -> () where A == 1 ? B == 1 : B == 0 {
}

// Simple main component to test parsing
comp main<'G: 1>() -> () {
  C1 := new Cond[1, 1];  // A=1, so B must be 1
}
