/// Butterfly module using existential parameters.
comp Foo[W]<'G: L>(
  in: ['G, 'G+1] W, // real and imaginary parts of input
) -> () with {
  some L where L > 0;
} {
  if W == 0 {
    L := 1;
  } else {
    L := 2;
  }
}