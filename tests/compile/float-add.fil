import "primitives/float.fil";

comp main<G: 2>(
  @interface[G] go_G: 1,
  @[G, G+1] left: 32,
  @[G, G+1] right: 32,
) -> (
  @[G, G+1] out: 32,
) {
  M0 := new FMult[32, 8, 23];
  M1 := new FMult[32, 8, 23];

  m0 := M0<G>(left, right);
  m1 := M1<G>(10, m0.out);
  out = m1.out;
}
