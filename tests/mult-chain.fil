import "primitives/core.fil";

component main<G>() -> () {
  M := new Mult;

  // Immediately use the value generated by the adder in the multiplier.
  m0 := M<G>(10, 20);
  m1 := M<G+3>(m0.out, m0.out);
}
